#server.error.whitelabel.enabled=false
spring.http.multipart.max-file-size=128MB
spring.http.multipart.max-request-size=128MB

#INFO The following two lines don't appear to have any effect
spring.mvc.view.prefix=/WEB-INF/jsp
spring.mvc.view.suffix=.jsp

#INFO Is this even a thing? It's a leftover from a tutorial. The IDE doesn't recognize it as something
#comming from Spring
spring.view.view-names=jsp/*

#INFO This line also doesn't appear to work
spring.resources.static-locations=classpath:/

management.endpoints.enabled-by-default=false
spring.datasource.username = root
spring.datasource.password = s7p5d10d20e1

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1

security.session = if_required

# Use for console application
# spring.main.web-environment=false
# spring.main.banner-mode=off

# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true



# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect
spring.jpa.properties.hibernate.jdbc.time_zone = UTC
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port = 80 

# Server compression
server.compression.enabled=true
#worker.compression.min-response-size=2048
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain,text/css,text/javascript,application/javascript
